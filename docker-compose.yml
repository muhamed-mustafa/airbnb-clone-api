services:
  mongodb:
    image: mongo:8.0
    container_name: mongodb
    restart: unless-stopped

    ports:
      - '127.0.0.1:27017:27017'

    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
